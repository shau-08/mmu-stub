# Controls what `make rtl-dispatch` (called by CD) actually runs.
# Edit these two values to change what a release build generates,
# without touching any .github/workflows/*.yml file.

RTL_TARGET = rtl
TARGET = MMU
